#pragma once

// Web-based authentication and loading system for HuhtalaHook
class WebAuth
{
public:
    // Starts the web authentication server and process
    // Returns true if authentication is successful
    static bool StartWebAuth();
};